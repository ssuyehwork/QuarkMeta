MainWindow 职责划分架构红线
本文档用于约束 AI 编码助手在涉及 MainWindow / 顶层窗口类的任何改动、重构、新增功能时必须遵守的判断规则。这不是风格建议，是硬性红线：任何改动落笔前，必须先用下面的判断法过一遍，过不了就不准写。

一、唯一判断标准（先背下来，再动手）
一段逻辑该不该放在 MainWindow 里，只问一句：这件事是不是"必须通过持有窗口顶层对象（QWidget/QMainWindow 本体）才能做到的事"？

能做到 → 允许留在 MainWindow
做不到，或者只是因为"这里最方便顺手连一下" → 禁止放在 MainWindow，必须下沉到对应的 Controller / Mediator
判断时不允许用"这样改动最小""这样不用改函数签名"之类的理由为自己开脱。改动量大小从不是判断依据，职责归属才是。

二、MainWindow 允许承担的职责（白名单，仅此五类）
1. 窗口物理属性与生命周期
只有持有窗口本体才能做的事：

showMinimized / showMaximized / close / 置顶（setWindowFlags/SetWindowPos）
窗口几何的保存与恢复（saveGeometry/restoreGeometry）
生命周期钩子：closeEvent、showEvent、changeEvent、resizeEvent、nativeEvent
无边框拖拽缩放（可委托给独立 Helper 类，但 Helper 需要窗口本体作为构造参数，MainWindow 是唯一合法宿主）
2. 对象图组装（Composition Root）
构造期一次性完成："谁先 new、按什么顺序 new、把哪些引用传给谁"。这一步做完立刻结束，不允许在组装之后继续插手这些对象之间如何协作。

3. 顶层子部件的容纳与排列
标题栏、导航栏、主体区、状态栏在一个布局容器里摞放的顺序——这是"长什么样"，属于 MainWindow。

4. 持久化的触发时机（不是持久化的内容）
"什么时候该存配置"（如关闭窗口时）是窗口生命周期的一部分，MainWindow 可以在正确的时间点触发保存动作。但"存哪个值、这个值来自哪个子组件的什么状态"，必须由拥有该状态的组件自己上报，MainWindow 不允许伸手进子组件内部去读取。

5. 只有 MainWindow 独占、其他任何控制器都拿不到的资源
例如 window() 指针本身、winId()。凡是需要这类资源的操作（如 FramelessWindowHelper 的调用），只能留在 MainWindow。

三、MainWindow 禁止承担的职责（黑名单）
1. 任何跨子组件的业务编排
"A 组件的某个操作要影响 B 组件的状态"——这是业务编排，不是窗口职责，无论看起来多小、多顺手，一律下沉到 Mediator/Coordinator/Controller。

判断口诀：只要连接的信号槽两端都不是"MainWindow 自己持有的窗口级资源"，而是两个业务/UI 组件互相之间的信号槽，这条连接就不该写在 MainWindow 里。

2. 子组件内部状态的读取或改写
MainWindow 不允许直接调用某个业务面板的具体业务方法（如 setZoomLevel、setViewMode、createNewItem 这类）。这些方法只能被"负责编排该面板与其他组件关系"的 Mediator 调用。

3. 为了图省事而"顺手多连一条线"
一旦发现自己正在 MainWindow 的某个初始化函数里，为了让两个原本已经通过 Mediator 连接好的组件之间再加一条新的直接信号槽连接——停下，这条新逻辑必须加进已存在的 Mediator，而不是加进 MainWindow。这是本文档要拦截的最高发违规模式。

4. 巨型初始化函数
一个函数里塞下"标题栏创建 + 面板创建 + 控制器创建 + 状态栏创建 + 全部信号连接"，即便每一行单独看都没有跨职责问题，函数本身也必须按阶段拆成独立私有方法。

四、常见违规模式对照表（AI 最容易犯的错）
违规模式	为什么错	正确做法
把两个业务组件的指针都传给 MainWindow 的某个初始化函数，在函数里直接 connect(a, ..., b, ...)	MainWindow 变成了隐性的编排器，编排逻辑散落在窗口装配代码里，无法集中审查	只把这两个组件的指针传给 Mediator 的构造函数，连接逻辑写在 Mediator 内部
发现"这个功能只用得到一次，专门建个 Mediator 方法太麻烦"，于是写成 MainWindow 里的一个 lambda	"只用一次"不是理由，下次同类需求会被同样的借口继续堆进 MainWindow，直到它重新变成上帝类	无论调用几次，只要是跨组件编排，一律进 Mediator
子组件（如标题栏）直接持有另一个业务组件的指针，调用其具体方法	子组件之间产生了横向耦合，脱离了"信号只能向外发、由更上层决定接给谁"的原则	子组件只允许对外发信号，不允许持有其他兄弟组件的指针
子组件内部直接读写全局配置/持久化单例	UI 组件承担了不属于它的持久化职责，之后想换存储方式要改动所有 UI 类	UI 组件只发信号上报状态变化，由 MainWindow 或专门的配置管理器决定何时以及如何持久化
重构时把"上帝类"拆成多个文件，但装配代码（组装 + 连接）仍然全部留在原来的大函数里	只做了"物理搬家"，没有做"职责转移"，本质问题原封不动	提取出来的每个 Controller/Mediator 类，必须真正拿到它该管的连接逻辑，不能只是换了个文件名字继续被 MainWindow 直接摆布
五、每次改动前的自查清单（改代码前必须逐条过一遍，过不了禁止提交）
这次新增/修改的信号槽连接，两端是不是都是"业务/UI 组件之间"的关系？如果是，是否已经放进对应 Mediator，而不是 MainWindow？
有没有为了"图方便"，让某个子组件持有了另一个兄弟组件的指针？
有没有让某个子组件直接读写全局配置/持久化单例，而不是通过信号上报？
如果这次改动让某个初始化函数变长了，是否需要拆出新的私有方法？
如果发现现有代码里已经存在类似违规（哪怕不是这次改动引入的），是否已经如实向用户报告，而不是绕开它继续叠加新代码？
这次改动是否只解决了用户明确要求的那个问题范围？有没有借机顺手改了没被要求的其他文件/其他功能？
任何一条过不了，禁止提交代码，必须先向用户说明冲突点，等待用户明确批准后再动手。

// ===================|===================

您好！我已经完成了对 MainWindow 及其关联类（TitleBarWidget、PanelMediator 等）的全面代码审计。以下是整理出的 违规清单与重构方案：

📋 MainWindow 及其关联类违规清单
一、 TitleBarWidget 违规项（src/ui/TitleBarWidget.h / .cpp）
违规点：子组件持有兄弟/其他组件指针，并直接调用其业务方法

位置：
TitleBarWidget.h: 行 22, 53, 54 (ContentPanel* m_contentPanel; PanelLayoutManager* m_layoutManager;)
TitleBarWidget.cpp: 行 21-40 (bindContentPanel, bindLayoutManager), 行 81-86 (m_btnLayout 点击事件中直接调用 m_layoutManager->showPanelContextMenu), 行 153-188 (setupViewMenu 中直接调用 m_contentPanel->currentViewMode() / setViewMode), 行 190-207 (setupCreateMenu 中直接调用 m_contentPanel->createNewItem)
违规类别：【黑名单】子组件互相持有指针 / 跨组件业务编排（违反对照表第 3 条："子组件直接持有另一个业务组件的指针，调用其具体方法"）。
重构建议：
从 TitleBarWidget 中彻底移除 m_contentPanel 和 m_layoutManager 指针及 bindContentPanel/bindLayoutManager 方法。
TitleBarWidget 转为纯 UI 视图，仅对外发射语义信号（如 zoomLevelChanged(int)、viewModeChanged(ViewMode)、createItemRequested(QString)、layoutMenuRequested(QPoint)）。
将所有与 ContentPanel 和 PanelLayoutManager 的交互联动逻辑下沉至 PanelMediator。
违规点：UI 子组件直接读写全局配置 / 持久化单例

位置：TitleBarWidget.cpp: 行 18 (AppConfig::instance().getValue("MainWindow/AlwaysOnTop")), 行 34 (AppConfig::instance().getValue("UI/GridZoomLevel")), 行 123 (AppConfig::instance().setValue("MainWindow/AlwaysOnTop"))
违规类别：【黑名单】子组件直接读写持久化（违反对照表第 4 条："UI 组件承担了不属于它的持久化职责"）。
重构建议：
移除 TitleBarWidget 中对 AppConfig 的直接读写。
缩放值与置顶状态的持久化，统一由 MainWindow（在生命周期钩子中）或 PanelMediator 负责，TitleBarWidget 仅通过信号/ Setter 接口暴露状态。
二、 MainWindow 违规项（src/ui/MainWindow.h / .cpp）
违规点：MainWindow 直接伸手读取/改写子组件内部控件状态

位置：
MainWindow.cpp: 行 107-111 (快捷键回调中通过 m_titleBarWidget->btnPinTop()->setChecked(...) 强行改写内部按钮状态)
MainWindow.cpp: 行 190-192 (changeEvent 窗口最大化/还原变化时，通过 m_titleBarWidget->btnMax()->setIcon(...) 强行改写标题栏内部按钮图标)
违规类别：【黑名单】子组件内部状态的读取或改写（违反黑名单第 2 条："MainWindow 不允许直接调用/伸手进子组件内部"）。
重构建议：
在 TitleBarWidget 中增加公有 Setter/状态更新方法（如 setPinned(bool)，updateMaximizedState(bool)）。
MainWindow 仅通过调用 TitleBarWidget 的公有方法来通知状态变化，不直接操纵内部 QPushButton。
违规点：在 MainWindow 里直接编排跨组件信号槽连接

位置：
MainWindow.cpp: 行 61-63 (connect(m_titleBarWidget, &TitleBarWidget::driveBarToggleRequested, ...) 控制 DriveBarWidget 显隐)
MainWindow.cpp: 行 94-97 (connect(m_searchController, &SearchController::searchExecuted, ...) 跨组件搜索编排)
MainWindow.cpp: 行 96 (m_searchController->bindContentPanel(m_contentPanel))
违规类别：【黑名单】跨组件业务编排 / 图省事顺手多连一条线（违反黑名单第 1、3 条）。
重构建议：
将 TitleBarWidget 与 DriveBarWidget 显隐联动、SearchController 与 ContentPanel / 状态栏的编排下沉到 PanelMediator 中统一进行。
违规点：巨型初始化函数

位置：MainWindow.cpp: 行 44-135 (MainWindow::initUi())
违规类别：【黑名单】巨型初始化函数（违反黑名单第 4 条）。
重构建议：
将 initUi() 拆分为 4 个高内聚私有辅助函数：
setupTopBars() （创建 TitleBar, NavBar, DriveBar）
setupCentralPanels() （创建 MainSplitter 和 5 大面板）
setupControllersAndMediators() （组装 PanelLayoutManager, PanelMediator, AppShortcutController 等）
setupStatusBar() （创建状态栏与绑定状态更新）
请审阅上述违规清单与重构方案。请确认是否批准按照此方案开始重构代码？

清单方向基本对，但有三处不够严谨，不批准现在就动手，需要先修正：

1. 持久化归属写得含糊，不允许"二选一"
你在 TitleBarWidget 违规项第二条的重构建议里写的是：

"缩放值与置顶状态的持久化，统一由 MainWindow（在生命周期钩子中）或 PanelMediator 负责"

这个"或"不能留。红线文档白名单第5条说得很清楚——只有需要 window() 才能完成的事才留在 MainWindow。这两个状态要分开判断：

置顶状态：pinToggled 信号触发后要调用 FramelessWindowHelper::setAlwaysOnTop(this, checked)，这一步必须拿到窗口本体，PanelMediator 拿不到。所以置顶的持久化 + 窗口置顶操作，明确归 MainWindow，不是可选项。
缩放初始值：读取 UI/GridZoomLevel 后同步给 ContentPanel 和 TitleBarWidget，这两个都是业务/UI 组件，不涉及窗口本体，明确归 PanelMediator，不是可选项。
给我明确的、不含"或"的归属结论，两条分开写。

2. 漏掉一处违规
MainWindow.cpp 里 AppShortcutController::toggleImmersiveRequested 连接到 m_panelLayoutManager->toggleImmersiveMode() 这一条，你的清单里没有列。这也是 MainWindow 在直接编排两个控制器（AppShortcutController 和 PanelLayoutManager）之间的关系，属于黑名单第1条，跟 togglePinRequested 那条（你已经列了）是同一类问题，必须一并列入、一并下沉。既然你说"深度排查、不可只查表面"，这种同类型的第二处遗漏说明排查还不够彻底，回去再过一遍 initUi() 之后所有 connect(m_shortcutController, ...) 的调用，不要只挑了一半。

3. 补充说明 PanelMediator 的构造函数改动
你的方案里多处提到"下沉至 PanelMediator"，但没有给出 PanelMediator 构造函数具体要新增哪些参数。请明确列出：新增参数列表（比如 TitleBarWidget* titleBar, DriveBarWidget* driveBar, PanelLayoutManager* layoutManager, AppShortcutController* shortcutController——你按实际需要的列，不要照抄我这个），以及 setupConnections() 里具体要新增哪几条 connect 语句、分别连接谁和谁。不需要现在写代码，只需要把这份接口层面的清单和第1、2点的修正一起补完，我确认后再放行你动手改代码。


感谢您的精准指正！我已经针对您提出的三点要求完成了修正与补充，修正后的完整方案如下：

一、 明确持久化归属（精确判断，无“或”）
置顶状态 (AlwaysOnTop) 的持久化与应用：
唯一归属：MainWindow
依据：置顶操作通过 FramelessWindowHelper::setAlwaysOnTop(this, checked) 实现，必须持有顶层 QMainWindow 本体。
机制：TitleBarWidget 发生置顶切换时发射 pinToggled(bool pinned) 信号给 MainWindow；MainWindow 在槽函数中调用 FramelessWindowHelper::setAlwaysOnTop(this, pinned)，并执行 AppConfig::instance().setValue("MainWindow/AlwaysOnTop", pinned) 写入配置；MainWindow 构造期读取配置恢复置顶状态。
缩放初始值 (UI/GridZoomLevel) 的持久化与应用：
唯一归属：PanelMediator
依据：缩放属于 UI 部件视图状态，不依赖窗口本体。
机制：在 PanelMediator::setupConnections() 中，从 AppConfig 读取初始缩放值同步给 ContentPanel 和 TitleBarWidget；当 TitleBarWidget 或 ContentPanel 缩放改变时，由 PanelMediator 编排双向同步，并负责统一写回 AppConfig::instance().setValue("UI/GridZoomLevel", value)。
二、 补充遗漏的快捷键与控制器连接排查
在 MainWindow.cpp 中排查到的所有 AppShortcutController 连接处理如下：

AppShortcutController::toggleImmersiveRequested（补充遗漏项）：
问题：原代码在 MainWindow 中连接 AppShortcutController 与 PanelLayoutManager（跨控制器编排）。
重构：彻底下沉至 PanelMediator，在 PanelMediator 内部建立 connect(m_shortcutController, &AppShortcutController::toggleImmersiveRequested, m_layoutManager, &PanelLayoutManager::toggleImmersiveMode)。
AppShortcutController::togglePinRequested：
问题：原代码在 MainWindow 中直接读取并修改 m_titleBarWidget->btnPinTop() 控件指针。
重构：MainWindow 监听 togglePinRequested 信号后，仅调用 m_titleBarWidget->setPinned(!m_titleBarWidget->isPinned()) 抽象公有接口，不再暴露内部 QPushButton。
三、 PanelMediator 构造函数及 setupConnections() 扩展清单
1. PanelMediator 构造函数扩展参数列表
在 PanelMediator.h 中新增相关组件指针（使用 QPointer 保护）：

explicit PanelMediator(NavPanel* navPanel,
                       FavoritePanel* favoritePanel,
                       ContentPanel* contentPanel,
                       MetaPanel* metaPanel,
                       FilterPanel* filterPanel,
                       AddressBar* addressBar,
                       SearchController* searchController = nullptr,
                       TitleBarWidget* titleBar = nullptr,
                       DriveBarWidget* driveBar = nullptr,
                       PanelLayoutManager* layoutManager = nullptr,
                       AppShortcutController* shortcutController = nullptr,
                       QObject* parent = nullptr);
2. setupConnections() 中具体下沉的 7 条连接/编排逻辑：
TitleBar 驱动 DriveBar 显隐： connect(m_titleBar, &TitleBarWidget::driveBarToggleRequested, m_driveBar, &DriveBarWidget::setVisible)
TitleBar 触发布局上下文菜单： connect(m_titleBar, &TitleBarWidget::layoutMenuRequested, m_layoutManager, &PanelLayoutManager::showPanelContextMenu)
TitleBar 改变 ContentPanel 视图模式： connect(m_titleBar, &TitleBarWidget::viewModeRequested, m_contentPanel, &ContentPanel::setViewMode)
TitleBar 触发 ContentPanel 新建文件/文件夹： connect(m_titleBar, &TitleBarWidget::createItemRequested, m_contentPanel, &ContentPanel::createNewItem)
TitleBar 与 ContentPanel 的缩放级别双向同步与持久化：
TitleBar 的 zoomLevelChanged(int) ➔ 同步 ContentPanel::setZoomLevel(int) + 写入 AppConfig("UI/GridZoomLevel")
ContentPanel 的 zoomLevelChanged(int) ➔ 同步 TitleBar::setZoomLevel(int)
SearchController 与 ContentPanel 绑定及状态更新：
m_searchController->bindContentPanel(m_contentPanel)
connect(m_searchController, &SearchController::searchExecuted, ...)
ShortcutController 触发沉浸模式切换： connect(m_shortcutController, &AppShortcutController::toggleImmersiveRequested, m_layoutManager, &PanelLayoutManager::toggleImmersiveMode)
请确认修改后的违规清单与接口扩展方案是否符合要求，期待您的放行批准！

可以放行。三点修正都站得住：

第1点的信号链路清晰，PanelMediator 不碰 UI 控件指针，MainWindow 只做纯粹的 setText 转发，边界干净。
第2点的判断依据是对的——纯布局显隐、无业务数据流转，符合白名单第3条，不是为了偷懒而留在 MainWindow，是真的分析过了再下的结论。
第3点你们自己发现并处理了一个细节我没提但需要注意的地方：PanelMediatorComponents 结构体里没有放 DriveBarWidget，跟第2点的结论保持了一致（既然 driveBar 联动留在 MainWindow，PanelMediator 就不该持有它），这说明前后两点结论是真的对齐了，不是两张皮。
批准开始执行重构。动手前最后提醒一句，按红线文档第五节的自查清单过一遍：

这次要写的每一条新 connect，两端是不是都对应到了刚才确认的归属结论（不是凭记忆，是对照上面这份清单逐条核对）？
改动范围严格限定在 TitleBarWidget、MainWindow、PanelMediator 这三处涉及的文件，PanelLayoutManager、FramelessWindowHelper 保持不动（前面已确认这两个文件本次无需改动）。
改完之后，TitleBarWidget.h 不应该再出现任何 ContentPanel、PanelLayoutManager、AppConfig 的前置声明或头文件引用——这是验证解耦是否彻底的最直接标志，编译完自己去确认一遍这几个符号是不是真的从 TitleBarWidget 里消失了。
改完后把完整代码发我审查，不要只发 diff 片段，我需要看到每个文件改完之后的全貌来确认没有遗漏或新引入的耦合。

// ===================|===================

这次终于把三个混在一起的症状拆清楚了，我逐个定性,然后指出一个更深层的架构性根因——不是三个孤立小 bug，是一个设计选型从根上就有问题。

症状1：启动时图标显示错误——已确认，前面诊断成立
restoreGeometry() 在 initUi() 最前面执行，此时 FramelessWindowHelper 还没 new 出来、m_titleBarWidget 也还不存在。如果上次关闭时窗口是最大化状态，restoreGeometry() 触发的 WindowStateChange 事件此刻打到 changeEvent() 里，if (m_titleBarWidget) 判空失败，图标同步被跳过——这条链路成立，维持原诊断。

症状2：启动即最大化时不能调整大小——这不是 bug，是正常行为，但暴露了另一个问题
一个真正处于最大化状态的窗口，本来就不应该能拖边缘调整大小（普通 Windows 窗口也是这样，必须先还原）。FramelessWindowHelper::eventFilter 里 !m_window->isMaximized() 这个判断守卫是对的。所以这一条本身不是 bug——但它间接说明了一件事：启动时如果窗口经由 restoreGeometry() 直接变成最大化，而这时 FramelessWindowHelper 还没挂载，WM_NCCALCSIZE/WM_GETMINMAXINFO 的多屏工作区补偿完全没生效，意味着这次启动时的最大化尺寸很可能是错的（比如盖住任务栏），只是你描述里没提这个细节，我不能替你确认，这个点先记下，等你之后验证。

症状3：还原后，从顶部/底部边缘拖拽错误触发最大化并闪烁——这才是真问题，根因在架构选型本身
我在 FramelessWindowHelper.cpp 里找到了确凿的结构性证据。看 handleNativeEvent 对 WM_NCHITTEST 的处理顺序：

if (!m_window->isMaximized() && !m_window->isFullScreen() && localPos.y() <= kBaseResizeMargin) {
    return false; // 顶部 8px 放行给 Qt
}

if (m_titleBar && !m_window->isFullScreen()) {
    QRect titleRect = ...
    if (titleRect.contains(localPos)) {
        ...
        *result = HTCAPTION;
        return true;
    }
}
标题栏高度是 34px，顶部"放行给 Qt"的缩放热区只有 8px，两者紧贴在一起、没有任何缓冲。这意味着：y ∈ [0,8] 被判定为"缩放区"（靠 Qt 侧手动处理），y ∈ (8,34) 被判定为 HTCAPTION（原生标题栏，交给 Windows 做原生拖动）。这两个区域的分界线只有一像素之隔——用户想去抓顶部边缘做缩放，鼠标只要落点稍微深一点（比如 y=10），系统就把这次按下识别成"原生标题栏拖动"而不是"缩放"。

原生标题栏拖动一旦被 Windows 接管，就会触发系统自带的 Aero Snap 特性：当一个通过 HTCAPTION 拖动的窗口被拖到屏幕顶部附近，Windows 会自动把它识别成"拖到顶部=最大化"的手势——这不是这份代码写错了什么判断条件，是 Windows 系统本身的行为，只要窗口的拖动被判定为 HTCAPTION，这个风险就存在。而你的窗口原本就贴着屏幕顶部（正常摆放位置），所以只要误判成标题栏拖动，几乎必然瞬间触发 Aero Snap 最大化，同时因为你的自定义无边框窗口没有走 DWM 原生的缩放合成路径，视觉上就会看到闪烁。

底部边缘同理会不会有一样的问题，目前证据不足，我不能像顶部这样下确定结论——底部没有标题栏重叠，代码里也没有专门针对底部的 WM_NCHITTEST 处理逻辑，所有非顶部、非标题栏命中的情况全部落到最后的 return false，跟顶部区域以外的处理完全一样。如果底部也出现同样的"触发最大化"现象，说明问题不只是"顶部与标题栏重叠"这一个诱因，可能还有别的东西在起作用（比如状态栏/TaskProgressToolBar 底部是否有其他事件抢占）。这一点我需要你确认：底部边缘拖拽触发最大化的现象，是每次必现，还是偶尔才发生？ 这决定了是同一个根因的镜像表现，还是需要单独排查的第二个问题。

更根本的问题：这套代码同时用了两套互相打架的缩放机制
这才是"从根本解决"该看的地方，不是去调整 8px 这个数字。现在的实现是：

原生层：靠 WM_NCHITTEST 返回 HTCAPTION 或 false，只处理"标题栏拖动"和"什么都不处理"两种情况
手动层：靠 QCoreApplication 全局事件过滤器里的 grabMouse + setGeometry 手动模拟四条边和四个角的缩放拖拽
行业内成熟的无边框窗口实现（VSCode、Windows Terminal 这类）不会用这种手动 grabMouse 方案，而是让 WM_NCHITTEST 对四条边和四个角分别返回 HTLEFT/HTRIGHT/HTTOP/HTBOTTOM/HTTOPLEFT/HTTOPRIGHT/HTBOTTOMLEFT/HTBOTTOMRIGHT，把缩放这件事完全交给 Windows/DWM 原生处理。 这样做的好处：

不存在"缩放区"和"标题栏拖动区"谁跟谁抢的问题——原生 hit-test 对每个像素只有一个确定归属，没有你现在这种"8px 缩放区贴着 34px 标题栏、中间没有缓冲"的先天冲突
缩放过程走 DWM 原生合成，不会出现手动 setGeometry 循环调用导致的闪烁
不需要在 QCoreApplication 级别装一个全局事件过滤器去拦截全应用的鼠标事件——这也是我们之前讨论过的、这个类本身架构上偏"暴力"的地方
现在这套手动实现，是"技术选型层面"的根本问题，不是某个数值调一调（比如把 8px 改成 16px）能治好的。 调数值只是把冲突区域挪个位置，冲突本身还在。真正的根治方案是把整个缩放机制换成原生 WM_NCHITTEST 返回精确的 HTxxx 值，彻底删掉 grabMouse/setGeometry 那套手动逻辑。

我需要把整个手动缩放机制换成原生 WM_NCHITTEST 方案，替换 FramelessWindowHelper 的核心实现