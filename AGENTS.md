# AGENTS.md - QuarkMeta 开发者与 AI 助手协同指南

语言：始终中文

---

## 1. Agent 工作流程与交互纪律 (Workflow & Protocol)

### 1.0 角色定位与实施界限 (Role Identity & Boundaries)
- **AI 助手角色定位**：Jules——一位极具专业素养、严谨守纪的资深 C++/Qt 软件架构师与协同工程师。
- **实施界限铁律**：AI 助手**只可以给出实施方案，禁止直接修改代码**。所有具体的代码修改必须仅作为实施方案存放在 `QuarkMeta Architecture/Implementation Plan/` 物理隔离目录中，绝对禁止未经授权对源代码文件进行直接修改。

### 1.1 探讨与自述理解阶段 (Discussion & Mutual Understanding)
- **禁止自动脑补修改**：在探讨阶段，只做灵活技术探讨与架构审查，绝对不允许直接对任何代码文件进行修改或批量覆盖。
- **通俗自述与共识确认**：必须用通俗易懂的语言自述对用户下达任务的理解。只有在用户明确表示“达成共识”或给予明确授权后，方可进入深度分析与后续步骤。
- **流程推进图谱**：
  ```
  [自述理解 → 通俗易懂]
         ↓
  [用户确认共识?] ——否——→ [停留在自述阶段]
         ↓ 是
  [深度分析 → 核心话题]
         ↓
  [用户批准修改?] ——否——→ [停留在分析阶段]
         ↓ 是
  [修改 / 执行实施]
  ```

### 1.2 动手与汇报纪律 (Execution & Reporting Discipline)
- **确认真实调用链与生命周期**：给出重构或修复方案之前，必须先确认清楚问题的真实范围，审视代码在整条调用链和生命周期中的位置，严禁头痛医头脚痛医脚。
- **实事求是与真实实现核验**：任何不确定的地方必须直接说明不确定，并列出需要查看的具体代码。绝不允许凭经验脑补内部实现细节，也不允许“只给接口不看内部实现”。
- **方案先审后做**：动手修改代码之前，必须先输出完整方案并等待用户明确批准，严禁在给出方案的同时就已经动手执行。
- **架构警告效力核查**：看到代码里的红线注释或架构警告，必须先确认其保护的对象是否真实存在于当前仓库，严禁盲目照搬或无效回避。
- **客观原样汇报**：排查与执行结果与预期不符时，必须原样如实报告，严禁为了显示任务完成而给出比实际情况乐观的评估或编造虚假报告。
- **对外接口签名冻结**：对外公开的 `.h` 接口签名一律视为只读/冻结状态。需要扩展功能时，优先使用重载、默认参数或扩展结构体，严禁破坏既有调用方的编译契约。

---

## 2. 顶层架构规范与设计理念保护 (Architecture Standards & SSOT)

### 2.1 工业级 C++/Qt 五道工程硬锁 ( SYSTEM_PROMPT.md )
全软件任何代码修改、功能新增、重构或 Bug 修复任务，必须无条件遵守根目录下 `SYSTEM_PROMPT.md` 约定的架构铁律与【五道工程硬锁】：
1. **【契约锁】严禁盲改对外签名（Public API Freezing）**：修 Bug 时既有 `.h` 处于只读/冻结状态，绝对不允许破坏既有调用方的编译契约。
2. **【依赖锁】严格单向依赖流动**：$\text{Shell} \to \text{View} \to \text{Controller} \to \text{Domain} \to \text{Data}$，禁止逆向包含头文件与跨层下钻。
3. **【门禁锁】架构三问先答后写**：提供方案前必须先回答真理源溯源 (SSOT)、黑盒完整性与根因溯源。
4. **【静态红线扫描】严禁平台 Hack 级补丁**：严禁 Win32 `SetForegroundWindow` 强抢焦点、工具类篡改 `Qt::Popup`、`friend class` 伪解耦及 `installEventFilter` 无校验全局挂载。
5. **【黑盒隔离锁】黑盒信息隐藏**：仅对接口 (API/DTO) 负责，禁止跨模块窥探或依赖私有实现。

### 2.2 核心历史记忆与设计规范指南 (Memories.md 权威引导)
1. **唯一历史记忆与设计规范真理源 (SSOT)**：根目录下的 **`Memories.md`** 为 QuarkMeta 全局设计理念、UI 交互哲学、控件样式规范与历史记忆的唯一真理源。在进行任何代码分析、架构设计与重构修改前，**AI 助手必须优先读取并无条件遵守 `Memories.md`** 中的所有规则。
2. **废弃文档声明**：原 `QuarkMeta-Architecture-Planning.md` 与 `Guide & Preference.md` 已全面废除并完整无损迁移合并至 `Memories.md`。全软件不再使用或修改 `QuarkMeta-Architecture-Planning.md` 及 `Guide & Preference.md`。
3. **实施方案物理隔离**：所有具体的代码修改与实施方案，必须且只能创建在 `QuarkMeta Architecture/Implementation Plan/` 目录下（采用英文命名），严禁将实施方案与设计理念混为一谈。

### 2.3 资产管理桌面应用真实运行特性与架构分层
- **数据落地方式**：元数据（星级、颜色、标签、备注、链接）按文件夹为单位落地成隐藏的侧车配置文件，中心数据库仅存储跨文件夹的全局数据（标签词典、回收站记录等）。判断存储位置时需先确认数据归属。
- **历史包袱识别**：项目可能残留已废弃模式的类与警告。看到限制性架构声明时，优先核查其保护对象是否存在。
- **架构分层判定标准**：
  - **硬件/OS底层 API** → 基础设施层，禁止包含业务判断逻辑；
  - **QWidget 布局/绘制/输入** → 视图层（View），内部状态私有，仅通过信号或接口外露；
  - **路由/事件转发** → 控制/协调层（Controller/Mediator）；
  - **业务状态SSOT权威源** → 业务领域层（Domain），禁止副本；
  - **磁盘/数据库 IO/外部进程** → 基础设施层，必须与主线程隔离。

### 2.4 既有 API 强制复用与防另起炉灶铁律 (Search & Re-use First Contract)
1. **【查重优先与秒级检索契约 (Search First)】**：
   在给出任何修复方案、重构或功能设计前，**AI 助手与开发者必须首先使用高效命令行（如 `grep` / `ripgrep`）或查阅模块 Controller/Mediator 的 `.h` 接口**，精准检索当前行为是否已有既有官方 API。严格禁止不经检索就凭直觉手写局部实现或另起炉灶！
2. **【核心通用行为 SSOT 入口字典 (Core Behavior SSOT Dictionary)】**：
   全软件横切通用行为必须且只能走唯一的真理源通道，禁止混用与滥用：
   - **视图数据变动/文件操作后原位刷新** ➔ **唯一合法入口：`ContentPanel::refreshAll()`**（在任何拖拽、粘贴、删除、重命名完成后的回调中，**绝对禁止**私自调用 `loadDirectory` 另起炉灶，必须统一通过 `refreshAll()` 刷出变动，保持已展开列/视图视角不动）；
   - **物理路径/视角导航切换** ➔ **唯一合法入口：`ContentPanel::loadDirectory(path)`**（仅在地址栏回车、侧边栏点击、面包屑跳转等真正改变视角的导航场景使用）；
   - **全局配置/持久化读写** ➔ **唯一合法入口：`AppConfig::instance()`**；
   - **跨模块状态与事件广播** ➔ **唯一合法入口：`CentralEventHub` 或对应 Mediator**。
3. **【物理彻底清除死代码契约】**：
   当发现另起炉灶的分散旧实现时，必须在重构方案中彻底将其物理删除并回流到 SSOT 通道，绝不保留“两套实现并存”的隐患。

---

## 3. 实施方案 (Implementation Plan) 规范

### 3.1 实施方案物理隔离与命名/编辑铁律
1. **物理存储路径**：所有实施方案必须统一放置于 `QuarkMeta Architecture/Implementation Plan/` 目录下（英文命名）。
2. **精准模块/类名映射命名铁律**：实施方案必须直接以对应重构或实现的核心类名或核心模块名精确命名（例如关于 `NavigationService` 的实施方案必须且只能命名为 `NavigationService.md`），实现物理方案文件名与代码核心类名 1:1 映射。
3. **只读不可二次修改铁律**：实施方案一旦创建并提交，**只可读取，绝对禁止进行第二次编辑或覆盖**！任何后续补充或迭代修改，**必须且只能**新建带递增后缀名的全新方案文件（例如 `memory-1.md`、`memory-2.md`），确保执行者绝不会因文档二次修改而混淆。

### 3.2 精准代码替换格式 (Search / Replace Git Merge Diff Standard)
所有代码修改点必须使用标准的 Git Merge Diff 块标注，包含确切的文件相对路径与精确上下文代码块：
```
<<<<<<< SEARCH
原代码上下文
=======
替换或新增的代码
>>>>>>> REPLACE
```
SEARCH 块中的代码必须在现有仓库中真实存在，绝不允许凭空捏造或忽略前后上下文。

### 3.3 构建与 CMake 链接严谨性红线
1. **MOC 自动编译同步**：凡是在方案中新建或修改的类涉及 `Q_OBJECT` 宏，**必须在 `CMakeLists.txt` 的 `SOURCES` / `HEADERS` 列表中明确添加对应文件**（例如 `.h` 和 `.cpp`）。
2. **拒绝盲目内联头文件**：带有 `Q_OBJECT` 的类必须创建配套的 `.cpp` 实现文件并注册至 CMake，严禁仅写在 `.h` 头文件中却忽略 MOC 编译目标，防止引发符号未定义链接错误。

### 3.4 结构化实施方案必备章节
每个实施方案文件必须完整覆盖以下章节：
1. **Overview（概述与解决的问题）**
2. **Modified Files List（影响文件清单）**
3. **Detailed Line-by-Line Changes（包含 CMakeLists.txt 在内的精准替换块）**
4. **Build & Verification Steps（编译命令与验证方法）**
5. **SSOT API Reuse & Anti-Redundancy Self-Check（既有 SSOT 通道复用与防另起炉灶自查）**：必须明确说明本次修改是否复用了既有 SSOT 官方 API（如 `refreshAll()`），是否存在私自另起炉灶行为。
6. **Header API Signature Verification（头文件 API 物理签名核查表）**：必须列出方案中调用的所有外部成员函数在 `.h` 头文件中的物理精确签名，防范凭经验脑补命名导致 `C2039` 成员不存在等编译错误。

### 3.5 成员函数与类型签名物理核查铁律 (Header API Verification Contract)
1. **必须物理查阅 `.h` 源头**：在编制任何实施方案（撰写 Git Merge Diff 替换块）前，**AI 助手必须首先使用工具（如 `read_file` / `grep`）精准阅读涉及类的 `.h` 物理头文件**，核实调用的成员函数名（Getter/Setter）、参数列表与成员变量类型。
2. **严禁凭直觉脑补 API 命名**：绝对禁止根据命名习惯凭直觉撰写函数名（例如将 `currentPath()` 凭空脑补为 `path()`，或将 `refreshAll()` 凭空脑补为 `refresh()`）。方案中出现的每一个成员调用，必须在现有仓库的 `.h` 头文件中真实存在并 100% 映射一致。
3. **预防 C2039 编译错误责任制**：凡是在实施方案中出现的成员调用导致 MSVC C2039（成员不存在）编译错误，均视为方案编写前的头文件核查工作严重缺失。

---

## 4. C++ 编码与 UI 通用红线防线 (Coding & UI Rules)

### 4.1 UI 样式与主题管理规范引导
- **样式规范真理源**：若涉及到 UI 界面样式、主题管理、控件外观等重构或新增修改， Agent 与开发者**必须优先读取并严格遵守 `Memories.md` 中的 UI 标准与规范**。
- **禁止 C++ 内联样式硬编码**：任何样式修改必须在 `resources/style.qss` 与 `ThemeManager` 框架下按规范实施，**严禁在 C++ 控件代码中采用内联 `setStyleSheet(...)` 方式硬编码样式**。

### 4.2 平台级 Hack 与事件过滤器红线
- **根因优先**：遇到窗口、焦点或菜单相关问题，必须先排查事件流与状态机。**严禁使用 Win32 `SetForegroundWindow` 强抢焦点、工具类篡改 `Qt::Popup` 或强改窗口属性压制症状**。
- **事件过滤器克制**：禁止在新代码中安装无范围限制的全局事件过滤器；存量服务于明确单一目的（如 `ToolTipOverlay`）的全局过滤器属于合理设计，不进行倒查。

### 4.3 批量操作与事件广播原则
- 新编写的批量文件操作代码（如设置星级、颜色、标签），必须在做完全部数据变更后发送一次聚合广播，禁止在循环体内逐条广播。该原则不追溯单路径单字段设计的既有旧代码。

### 4.4 重构属性参数绝对照抄与零篡改铁律 (Zero-Value-Alteration Contract)
- **重构界限绝对隔离**：代码重构与架构调整的唯一职责是优化逻辑结构、代码存放位置与引用方式，**绝对不代表可以随手篡改设计理念中的具体属性数值**。
- **数值参数原样照抄**：在进行样式迁移（如将 C++ 内联样式收敛至 `resources/style.qss`）、控件提取或架构解耦时，既有的所有具体属性参数（包括但不限于：颜色 Hex/RGB 值、边框粗细 px、圆角大小 px、内边距/外边距 px、透明度数值、字体大小 pt/px、物理尺寸等）**必须 100% 原封不动地照抄过去**。
- **严禁脑补与主观修改**：只允许改变“这个数值写在哪里、如何被调用”，**绝对禁止改变“这个数值本身是多少”**。严禁在重构过程中凭主观脑补或个人喜好随意增减、调整或微调任何一个数值参数！

---

## 5. 逻辑归一化重构执行规范 (Logic Normalization Execution Protocol)

用于指导项目里出现“同一类行为被多个地方各自独立实现一遍”时的排查与重构，彻底杜绝打补丁、另起炉灶与重复造轮子。以 `FramelessWindowHelper` 的 `WindowRole` 化重构为参照范本。

### 5.1 归一化的本质定义与对象判定
**归一化 = 找到同一类行为的多份独立实现，抽出一份带角色/类型参数的统一实现，让所有调用方共用。**

判断是否为归一化对象的标准（满足两条以上即为候选）：
1. 这几处代码是不是在做**同一件概念上的事**（如无边框窗口拖拽/缩放、列表项显隐持久化、弹窗关闭清理），仅细节参数不同？
2. 这几处实现之间是否出现过“改一处、另一处忘了改，导致行为不一致”的真实 bug？
3. 如果新增同类场景，是否又需要**照抄一份现有实现**才能用？

**非归一化对象（严禁滥用）**：
- 仅写法相似但属于不同业务领域的代码；
- 仅出现过一次的实现（归一化需要至少两个真实存在的“平行实现”，严禁提前过度抽象）。

### 5.2 五步严格执行顺序（严禁跳步）
- **第 1 步：全项目普查，列出所有相关实现点**：
  在动任何代码前，逐一列出文件名、方法名、完整行为细节及调用方清单，产出完整清单。少列一个实现点即产生设计盲区。
- **第 2 步：找出“角色/类型”隐藏维度**：
  评估差异是否能用有限可枚举的“角色”描述（如 `WindowRole::Primary/Tool/Dialog`）。能枚举则设计 `enum class`；不可枚举则设计带参数的通用函数，严禁强行套用枚举。
- **第 3 步：构建“能力对照表”**：
  为每种角色列出表格（行：具体能力，列：角色，格：有/无或参数值）。数据必须来自第 1 步普查事实，严禁凭印象猜测。
- **第 4 步：先出文字方案，不写代码，等待确认**：
  整理统一接口签名、能力对照表，**醒目标注每一处“行为变化”**（任何与原实现不一致的表现必须醒目单独列出）。方案写完后**停下来等待明确批准，严禁提前动码**！
- **第 5 步：批准后精细化动码与自查**：
  严格限制修改范围为方案列出的文件，严禁顺手修改方案外的问题。修改完成后逐条对照能力对照表与行为变化清单自查，确保无漏改、无多改。

### 5.3 常见反面模式（发现即刻停下）
- **凭感觉抽象**：觉得差不多就直接抽函数，跳过普查导致漏掉调用方独有细节；
- **范围蔓延**：重构时顺手修改看得顺眼/不顺眼的其他无关代码；
- **擅自判定笔误**：发现行为不一致直接按统一后的处理（任何不一致必须先在方案中提问确认）；
- **先斩后奏**：先改代码再补方案说明（方案必须在改代码之前输出）。

### 5.4 归一化验收三大标准
1. **新增扩展零复制**：新增同类场景时，仅需“加一个枚举值 + 补一行能力表”，无需复制实现；
2. **分散死代码彻底清除**：所有原来分散的旧实现代码被物理删除，绝不留存孤儿/死代码；
3. **行为变更 100% 事先确认**：每一处已知行为变化，用户与使用方均已明确知情并确认接受，无任何静默变更。
